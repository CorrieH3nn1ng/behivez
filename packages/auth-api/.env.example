DATABASE_URL="postgresql://postgres:password@localhost:5432/behivez_auth?schema=public"
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"
PORT=3100
NODE_ENV=development

# Email — Mailpit in dev (no auth needed)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_FROM="BeHivez <noreply@behivez.co.za>"

# Password reset page URL (frontend)
RESET_PASSWORD_URL="http://localhost:8090/auth/reset-password"
